task: trajectory
processed_dir: /mnt/data/processed/traj_w96_h20
out_dir: /mnt/data/checkpoints
window: 96
horizon: 20
features: [dx, dy, sog, cog_sin, cog_cos, accel, dt, cell_id]
model:
  name: tptrans
  d_model: 192
  nhead: 4
  enc_layers: 4
  dec_layers: 2
loss: huber
optimizer: adamw
lr: 0.0003
batch_size: 96
epochs: 5
